import NemesisCommand from "../../../../components/NemesisCommand";

export default function NemesisCommandPage() {
  return (
    <div>
      <div className="page-title">NEMESIS Command</div>
      <div className="page-subtitle">early playable build</div>
      <NemesisCommand />
    </div>
  );
}
